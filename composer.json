{
	"name": "macros-by-sara/wp-app",
	"description": "a custom wordpress theme that powers the Macros By Sara web app",
	"type": "project",
	"repositories": [
		{
			"type": "composer",
			"url": "https://wpackagist.org",
			"only": [
				"wpackagist-plugin/*",
				"wpackagist-theme/*"
			]
		}
	],
	"scripts": {
		"phpcs": "phpcs functions.php inc/ --extensions=php --ignore=./build/",
		"phpcbf": "phpcbf functions.php inc/ --extensions=php --ignore=./build/"
	},
	"config": {
		"allow-plugins": {
			"composer/installers": true,
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	},
	"require-dev": {
		"wp-coding-standards/wpcs": "*"
	}
}
